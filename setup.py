from setuptools import setup, find_packages

setup(
    name='rct',
    version='0.1',
    description='Compute the RCT (Relevance–Cost–Time) index for evaluating AI systems',
    author='Tin Popović',
    packages=find_packages(),
    install_requires=[],
    python_requires='>=3.7',
)
